[package]
name = "vbash-downstream-smoke"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
publish = false

[dependencies]
vbash = { path = "../../crates/vbash-compat" }

# Keep this fixture outside the repository workspace so Cargo resolves a fresh
# downstream graph without inheriting the repository's Cargo.lock.
[workspace]
