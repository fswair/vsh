[package]
name = "vsh-monty"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
readme.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Typed Monty OS-call and high-level tool adapter for the VSH virtual filesystem"
publish = ["crates-io"]

[dependencies]
# Keep the direct accounting edge on the exact patch used by Monty 0.0.22's
# Ruff 0.0.9 graph so workspace resolution cannot silently drift.
get-size2.workspace = true
monty.workspace = true
monty-proto.workspace = true
monty-types.workspace = true
vsh-policy.workspace = true
vsh-types.workspace = true
vsh-vfs.workspace = true

[dev-dependencies]
vsh-store.workspace = true

[lints]
workspace = true
